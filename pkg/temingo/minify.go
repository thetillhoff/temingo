package temingo

func (engine Engine) minify(content []byte, ext string) []byte {

	switch ext {
	// TODO
	default:
		// if engine.Verbose{
		// 	log.Println("beautified",ext)
		// }
		return content
	}
}
